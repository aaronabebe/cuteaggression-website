{
  "name": "gullible-gamma",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node src/lib/embed.test.ts",
    "deploy": "ssh -n uberspace 'cd /home/aaronabb/websites/cuteaggression-website && echo :: 1/4 pull && git pull --ff-only origin main && echo :: 2/4 install && npm install && echo :: 3/4 build && npm run build && echo :: 4/4 publish && rsync -av --delete --progress dist/ /var/www/virtual/aaronabb/www.cuteaggression.lol/ && echo :: deployed'"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^6.0.3"
  }
}
